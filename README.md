# Disposable LSDO publications target

Private, synthetic-only target used to test the one-surface publication transaction.
This is not the permanent publications repository. It must never contain an audited
paper or become public.

Promotion pull requests are accepted only when their exact changed bytes match an
authenticated authorization envelope. The trusted workflow has read-only
permissions and cannot merge or modify repository contents.
